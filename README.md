Installs apps, sets up dotfiles, development environment, theme. 

Pick and choose what you want to install, or get the lot.

After a fresh ubuntu install run:

```sh
./ubuntu/setup.sh
```

On OSX:

```sh
./osx/setup.sh
```

